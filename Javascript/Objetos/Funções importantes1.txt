1) Object.Keys(obj) //retorna as chaves de um objeto

2) Object.value(obj) //retorna os elementos

3) Object.entries(obj) = coloca os elementos em um array

//define um elemento e seta propriedades
4) Object.defineProperty(obj, 'elemento'){
    enumerable: //se pode ser listado
    writable: //se pode ser redefinido
}

5) Object.assign(destino, a, b) //concatena objetos

6) Object.freeze(obj) //congela objetos